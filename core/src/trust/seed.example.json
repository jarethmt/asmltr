{
  "_comment": "Copy to seed.json (gitignored), then run `node core/src/trust/seed.js`. Trust is DEFAULT-DENY. Ivy localhost: principal id `owner` is James Watt. Web chat + asmltr ask post channel assistant-web; core stamps sender.raw_id from ASMLTR_WEB_OWNER_ID (fallback X-Remote-User). Seed that same value as an assistant-web identifier. CLI is not a trust channel (control actor is cli:adjutant@<tty>). Do not invent Discord snowflakes — add a real discord id later.",
  "roles": [
    {
      "id": "readonly",
      "name": "Read Only",
      "allow": ["read", "search", "recall"],
      "forbidden": ["write", "deploy", "credentials"]
    }
  ],
  "principals": [
    {
      "id": "owner",
      "display_name": "James Watt",
      "notes": "Owner / full trust. Local assistant-web only until Discord is added later.",
      "identifiers": [
        { "surface": "assistant-web", "value": "owner" }
      ],
      "grants": [ { "bypass_moderation": true, "allow": ["*"] } ]
    }
  ]
}
